class Client {
    constructor(email,name,sum_orders,quantity_orders) {
      this.email=email
      this.name=name
      this.sum_orders=sum_orders;
      this.quantity_orders=quantity_orders
    }
};

module.exports = {Client}
